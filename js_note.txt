js笔记：

0.语法结构：
	变量/数据类型+操作符+关键字+注释+控制语句(普通语句+循环+判断)（+Unicode字符集）

1.数据类型：(变量声明——var，存储信息的容器，作用域&生命周期)
	字符串string，
	数字num，
	逻辑bool，
	数组array，
	对象object，json：
	函数function，

2.控制语句：
	循环：
	判断：
	
	
3.

N.进阶教程：
	ajax，
	highchart，
	jQ，
	

A.基本操作：
	显示数据：
		window.alert()——弹出警告框/document.write()——方法将内容写到HTML文档中/innerHTML 写入到 HTML 元素/使用 console.log() 写入到浏览器的控制台。
	